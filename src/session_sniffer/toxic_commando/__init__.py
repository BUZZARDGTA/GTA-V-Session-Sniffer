"""John Carpenter's Toxic Commando process detection and state management.

Groups modules dedicated to the John Carpenter's Toxic Commando process:

- `process`: detect the running Toxic Commando executable and snapshot its state
    (`ToxicCommandoStatus`, `find_running_toxic_commando_path`).

Import symbols directly from their submodules
(e.g. `from session_sniffer.toxic_commando.process import ToxicCommandoStatus`).
"""
